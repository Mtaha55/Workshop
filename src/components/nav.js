import React from 'react';
import './style.css';

const Nav = () => {
    return (
        <nav className="nav">
            <a href="/">Login</a>
            <a href="/">Sign Up</a>
            <a href="/">About</a>
        </nav>
    );
};

export default Nav;
